package com.example.toyracers.race

/** Explicit race-domain name retained as an alias for source compatibility. */
typealias RacePositionTracker = PositionTracker

/** Coordinates start, simulation, progress and finish for a race. */
internal typealias RaceManager = RaceSession
