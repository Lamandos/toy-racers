# Compatibility schemas

The versioned JSON Schema documents in this directory define the portable scenario, input-script,
normalized snapshot, and complete golden-trace contracts used by compatibility scenarios and
goldens. `trace.schema.json` describes the trace envelope and references `snapshot.schema.json` for
each sampled snapshot.
