# Compatibility schemas

The versioned JSON Schema documents in this directory define the portable scenario, input-script,
and normalized snapshot contracts used by compatibility scenarios and goldens.
